__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

.env
.env.*
!.env.example
# Contains no secrets (just VITE_API_URL= empty, overriding the local-dev backend URL for
# production builds) -- committed so every production build gets this right by default,
# regardless of whether Vercel's dashboard env vars are configured correctly.
!frontend/.env.production

.pytest_cache/
.coverage
htmlcov/

.DS_Store
*.log

.vscode/
.idea/

.claude/
